/**
navigateTo(element)
* @description scrolls an element into viewport
* @param {html element} element
*/

/**
makeActive()
* @description Checks if an element is in the viewport and adds the active class to it else removes the class
*               also highlights the nav link to the section if in viewport.
*                This is a callback function passed to an event listener for the document so that it is called whenever is scrolling.
*/

/**
setLinkEvents()
* @description Sets events for nav links to scroll the elements coresponding to each link into view
*/
